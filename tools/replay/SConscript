Import('env', 'arch', 'common', 'messaging', 'visionipc', 'cereal')

replay_env = env.Clone()

# Vendored ffmpeg (shared with selfdrive/ui/qt/screenrecorder/ffmpeg,
# committed in 43c51f7). host tool, 仅需 CPPATH/LIBPATH.
ffmpeg_root = '../../selfdrive/ui/qt/screenrecorder/ffmpeg'
replay_env['CPPPATH'] += [ffmpeg_root + '/include']
replay_env['LIBPATH'] = [ffmpeg_root + '/lib'] + replay_env.get('LIBPATH', [])
# host 工具, 仓库路径不定: 运行期用 $ORIGIN(相对可执行文件) 而非绝对路径
replay_env.Append(LINKFLAGS=['-Wl,-rpath,\'$$ORIGIN/../../selfdrive/ui/qt/screenrecorder/ffmpeg/lib\'',
                              '-Wl,--no-as-needed'])

replay_env['CCFLAGS'] += ['-Wno-deprecated-declarations']

base_frameworks = []
base_libs = [common, messaging, cereal, visionipc, 'm', 'ssl', 'crypto', 'pthread']

if arch == "Darwin":
  base_frameworks.append('OpenCL')
else:
  base_libs.append('OpenCL')

replay_lib_src = ["replay.cc", "consoleui.cc", "camera.cc", "filereader.cc", "logreader.cc", "framereader.cc",
                  "route.cc", "util.cc", "seg_mgr.cc", "timeline.cc", "api.cc"]
replay_lib = replay_env.Library("replay", replay_lib_src, LIBS=base_libs, FRAMEWORKS=base_frameworks)
Export('replay_lib')
replay_libs = [replay_lib, 'avutil', 'avcodec', 'avformat',
                'swresample', 'x264',
                'bz2', 'zstd', 'curl', 'yuv', 'ncurses'] + base_libs
replay_env.Program("replay", ["main.cc"], LIBS=replay_libs, FRAMEWORKS=base_frameworks)

if GetOption('extras'):
  replay_env.Program('tests/test_replay', ['tests/test_replay.cc'], LIBS=replay_libs)
